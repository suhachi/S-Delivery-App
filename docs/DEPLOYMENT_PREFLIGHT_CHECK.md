# ⚠️ 필수 배포 전 점검 (Mandatory Pre-Flight Check)

**모든 배포(Deployment) 작업 이전에 반드시 수행해야 하는 계정 및 프로젝트 확인 절차입니다.**
잘못된 계정으로 배포를 시도하거나, 엉뚱한 프로젝트에 코드를 덮어쓰는 사고를 방지하기 위해 이 절차를 준수하십시오.

---

## 🛑 CHECK 1: 현재 로그인 계정 확인 (Who am I?)

터미널에서 아래 명령어를 실행하여 현재 로그인된 Google 계정을 확인합니다.

```bash
firebase login
```
- **확인 사항**:
    - `Already logged in as [계정명]` 메시지가 표시됩니다.
    - **[계정명]**이 해당 프로젝트의 **Owner(소유자)** 또는 **Editor(편집자)** 권한이 있는 계정인지 눈으로 확인합니다.
    - *권한이 없는 계정이라면 즉시 `firebase logout` 후 `firebase login`으로 다시 로그인하십시오.*

---

## 🛑 CHECK 2: 프로젝트 접근 권한 및 ID 확인 (Where am I going?)

현재 계정에서 배포하려는 프로젝트가 실제로 보이는지 확인합니다.

```bash
firebase projects:list
```
- **확인 사항**:
    - 목록에 **배포 대상 Project ID**가 포함되어 있어야 합니다.
    - 예: `simple-delivery-app-9d347` 또는 `hyun-poong` 등.
    - *목록에 없다면, 현재 계정은 해당 프로젝트에 대한 접근 권한이 없는 것입니다.*

---

## 🛑 CHECK 3: 타겟 프로젝트 설정 확인 (Target Check)

`.firebaserc` 파일이 올바른 프로젝트를 가리키고 있는지 확인합니다.

```bash
# Windows (PowerShell)
type .firebaserc

# Mac/Linux
cat .firebaserc
```
- **확인 사항**:
    - `"default": "프로젝트-ID"` 부분이 **CHECK 2**에서 확인한 ID와 일치해야 합니다.

---

## ✅ GO
위 3가지가 모두 일치할 때만 아래 명령어를 실행하십시오.

```bash
firebase deploy --only hosting,functions
```
