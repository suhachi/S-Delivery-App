# 프로젝트 마스터 스냅샷 v1.0.0 - functions/package-lock.json (전체 원본)

**생성일**: 2025-12-10  
**목적**: functions/package-lock.json 전체 원본 보관  
**파일 크기**: 약 10,000줄 이상

---

## 중요 안내

`functions/package-lock.json` 파일은 매우 큽니다 (약 10,000줄 이상).  
전체 내용은 별도 텍스트 파일로 저장되어 있습니다:
- `functions-package-lock-json-raw.txt` - functions/package-lock.json 전체 원본

**실제 프로젝트 복제 시 권장 방법**:
1. 원본 `functions/package-lock.json` 파일을 직접 복사하거나
2. `cd functions && npm install`을 실행하여 자동 생성

---

## functions/package-lock.json 구조 요약

```json
{
    "name": "functions",
    "lockfileVersion": 3,
    "requires": true,
    "packages": {
        "": {
            "name": "functions",
            "dependencies": {
                "firebase-admin": "^11.5.0",
                "firebase-functions": "^4.3.1"
            },
            "devDependencies": {
                "@types/node": "^18.0.0",
                "@typescript-eslint/eslint-plugin": "^5.12.0",
                "@typescript-eslint/parser": "^5.12.0",
                "eslint": "^8.9.0",
                "eslint-config-google": "^0.14.0",
                "eslint-plugin-import": "^2.25.4",
                "firebase-functions-test": "^3.0.0",
                "typescript": "^4.9.0"
            },
            "engines": {
                "node": "18"
            }
        },
        // ... (수천 개의 패키지 정의)
    }
}
```

---

## 주요 의존성 패키지 목록

### Dependencies
- firebase-admin (^11.5.0)
- firebase-functions (^4.3.1)

### DevDependencies
- typescript (^4.9.0)
- @types/node (^18.0.0)
- @typescript-eslint/* (ESLint 플러그인)
- eslint (^8.9.0)
- eslint-config-google (^0.14.0)
- firebase-functions-test (^3.0.0)

---

## 전체 원본 파일

전체 내용은 `functions-package-lock-json-raw.txt` 파일에 저장되어 있습니다.  
이 파일은 원본 `functions/package-lock.json`의 정확한 복사본입니다.

---

**원본 파일 위치**: `functions/package-lock.json`  
**전체 원본 저장 위치**: `functions-package-lock-json-raw.txt`


